[pytest]
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    adaptive: marks tests that exercise the opt-in adaptive context profile
    serial
    kvarn_local: requires a compatible local KVarN target model and GPU
